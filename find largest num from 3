#include <stdio.h>

int main() {
    int a, b, c;
    scanf("%d %d %d", & a, & b, & c);

    if (a > b && a > c)
    {
        printf("Largest: %d\n", a);
    }

    else if (b > a && b > c)
    {
        printf("Largest: %d", b);
    }

    else if (c > a && c > b)
    {
        printf("Largest: %d\n", c);
    }

    else if (a == b && b == c)
    {
        printf("");
    }
    
    
     else if (a<b && b == c)
    {
        printf("Largest: %d\n", b);
    }
    
      else if ( a == c)
    {
        if (b<a)
        printf("Largest: %d\n", a);
        else 
        printf("Largest: %d\n", b);
    }
    
    
     else if ( a == b)
    {
        if (c<a)
        printf("Largest: %d\n", a);
        else 
        printf("Largest: %d\n", c);
    }
    
    else if ( c == b)
    {
        if (c<a)
        printf("Largest: %d\n", c);
        else 
        printf("Largest: %d\n", a);
    }

}
